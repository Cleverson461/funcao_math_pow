function pontencia(n1, n2){
  let result = Math.pow(n1, n2);

  return result
}

document.write(`A potencia: ${pontencia(2, 2)}<br/>`)
document.write(`A potencia: ${pontencia(3, 2)}<br/>`)
document.write(`A potencia: ${pontencia(18, 2)}`)